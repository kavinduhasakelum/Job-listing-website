# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=job_portal

# Server
PORT=3000
JWT_SECRET=your_jwt_secret
CLIENT_URL=http://localhost:5173

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# SMTP / Email
# For Gmail: enable 2-Step Verification and create an App Password to use below.
# Alternatively specify SMTP_HOST/SMTP_PORT for providers like Mailtrap, SendGrid, etc.
SMTP_SERVICE=gmail
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_SECURE=false
SMTP_USER=your_email@example.com
SMTP_PASS=your_smtp_or_app_password
# SMTP_FROM="Job Portal <notifications@example.com>"
